# M1: 服务器核心 ✅

**状态**：已完成

**目标**：可启动的 FastAPI 服务器，有完整的项目结构、数据库模型、LLM 网关和单 Agent 运行时。

## 阶段 1.1 项目骨架

- [x] FastAPI 应用可启动，`/health` 端点返回 200
- [x] 目录结构符合约定
- [x] 配置通过环境变量 / `.env` 文件管理
- [x] Alembic 可执行数据库迁移
- [x] `pyproject.toml` 包含所有依赖
- [x] Docker Compose 可启动 PostgreSQL + Redis

## 阶段 1.2 数据库模型

- [x] 定义模型：Project, Agent, LLM, Task, Lock, ProjectMemory, GlobalMemory, AuditLog
- [x] Alembic 生成迁移脚本并成功执行
- [x] 模型关系正确定义（外键、一对多）
- [x] 提供 seed data 脚本

## 阶段 1.3 LLM 网关适配器

- [x] `LLMGateway` 类，支持 OpenAI 和 Claude 格式
- [x] 异步调用（httpx）
- [x] 错误处理（429 限流、超时、网络错误）
- [x] 配置文件定义模型列表和网关地址
- [x] 单元测试覆盖

## 阶段 1.4 Agent 运行时

- [x] `Agent` 类实现，持有上下文、工具集、LLM 配置
- [x] Agent 可接收任务描述，调用 LLM 执行，返回结果
- [x] Agent 状态管理（idle → running → completed / failed）
- [x] 上下文窗口管理（基于 API 返回的实际 token 消耗，自动 compact）
- [x] `ToolExecutor` 实现，支持动态注册工具
- [x] 内置工具：`file_read`, `file_write`, `shell`
- [x] `AgentRepository` 实现，Agent 不直接操作数据库
- [x] 单元测试覆盖（64 个测试通过）
